## db.setting文件

url = jdbc:mysql://101.200.138.97:3306/nybikedb
user = remoteselect
pass = shabi111.

## 可选配置
# 是否在日志中显示执行的SQL
showSql = true
# 是否格式化显示的SQL
formatSql = false
# 是否显示SQL参数
showParams = true
# 改成false后打印日志会简洁很多
# 打印SQL的日志等级，默认debug，可以是info、warn、error
sqlLevel = debug